# GitHub metadata

Suggested repository description:

`Python ETL and migration CLI for payroll data: Oracle extraction, validation, normalized PostgreSQL loading, and JSON cache refresh workflows.`

Suggested short profile mention:

`Built a payroll migration engine in Python to extract, validate, normalize, and load high-volume payroll data from Oracle into PostgreSQL.`
